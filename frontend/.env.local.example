# Seed Finance - Local Anvil Development Environment
# Copy this file to .env.local and fill in addresses from DeployLocal.s.sol output
#
# Usage:
#   cp .env.local.example .env.local
#   # Run deployment script, then paste addresses below
#   npm run dev

# Environment - set to "local" for Anvil testing
NEXT_PUBLIC_ENV=local

# WalletConnect Project ID (any value works for local testing)
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=demo-project-id

# ============================================
# Contract Addresses (from DeployLocal.s.sol)
# ============================================
# After running the deployment script, copy the addresses here:
#
# forge script script/DeployLocal.s.sol:DeployLocal \
#   --rpc-url http://localhost:8545 \
#   --private-key 0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80 \
#   --broadcast

# MockUSDC contract address
NEXT_PUBLIC_USDC_ADDRESS=

# LiquidityPool (ERC-4626 vault) contract address
NEXT_PUBLIC_LIQUIDITY_POOL_ADDRESS=

# InvoiceDiamond (Diamond proxy) contract address
NEXT_PUBLIC_INVOICE_DIAMOND_ADDRESS=

# ExecutionPool contract address
NEXT_PUBLIC_EXECUTION_POOL_ADDRESS=

# Optional - Treasury Manager (if deployed)
NEXT_PUBLIC_TREASURY_MANAGER_ADDRESS=

# Optional - LI.FI Receiver (if deployed)
NEXT_PUBLIC_LIFI_RECEIVER_ADDRESS=
